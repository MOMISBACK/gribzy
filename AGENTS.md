# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

# Sources of truth

Read `docs/APP_REFERENCE.md`, then the linked `docs/PRODUCT.md`,
`docs/ARCHITECTURE.md`, and `docs/IMPLEMENTATION_PLAN.md` before planning or changing
the application. Keep the product
document current for vision, UX, roadmap and limitations. Keep the architecture
document current for stack, data formats, services and implementation rules.
